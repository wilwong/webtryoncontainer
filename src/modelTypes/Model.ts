type Model = {
    image: string
    status: string
    localizedNames: Record<string, string | undefined>
    props: any
    modelFile: string
    category: string
    name: string
}

export default Model