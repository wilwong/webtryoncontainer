type Category = {
    image: string
    localizedDetails: { [key: string]: string }
    localizedTitles: { [key: string]: string }
    status: string
    name: string
    promoted: Boolean
}

export default Category